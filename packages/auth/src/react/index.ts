/**
 * @workspace/auth/react
 *
 * TanStack Query hooks for Better Auth — queries and mutations.
 * Import from this entry point instead of @better-auth-ui/react directly
 * so the auth package is the single source of truth.
 */

export * from "./queries"
export * from "./mutations"
