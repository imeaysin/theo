# Next.js type resolution breaks under pnpm's default isolated linker when
# @babel/core is present in the workspace (via eslint, shadcn, etc.). Hoisted
# linking ensures a single correct next install for all apps.
node-linker=hoisted
